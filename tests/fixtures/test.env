# Test salt for tests/run.sh. A real install keeps 32 random bytes here, mode 600:
#   printf 'SALT=%s\n' "$(od -An -tx1 -N32 /dev/urandom | tr -d ' \n')" > /etc/diskvet.env
SALT=0123456789abcdef0123456789abcdef0123456789abcdef0123456789abcdef
